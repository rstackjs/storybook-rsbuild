import { PackageManagerTabs } from '@theme'

# Next.js

Storybook for Next.js & Rsbuild 通过 `next-rspack` 把 Storybook 的 preview runtime 桥接到 Next.js 自己的构建管线上，让你能在隔离环境中开发 Next.js 组件，同时复用与应用完全相同的 loaders、SWC 选项和 `next.config.ts` 配置。

这个 framework **不引入任何自有的构建配置语言**：你照常通过 `next.config.ts` 配置应用，bridge 会原样复用它 —— [配置心智模型](#configuration-mental-model)会讲清楚任意一项设置该写在哪里。

:::warning 实验性
`storybook-next-rsbuild` 依赖 Next.js 内部实现，并跟随 Next.js 的发布节奏演进。任何 `next` 的 minor 或 patch 升级都可能破坏兼容性 —— 请锁定 `next` 以保证可复现性，并让 `storybook-next-rsbuild` 与 `next` 一起升级。
:::

## 环境要求 {#requirements}

<div style={{ width: '100%', maxWidth: '460px' }}>

| Package | Version |
| :--- | :--- |
| `next` | `^15.3.0 \|\| ^16.0.0` |
| `next-rspack` | 与 `next` 相同(同一版本) |
| `react` / `react-dom` | `^18.0.0 \|\| ^19.0.0` |
| `storybook` | `^10.3.5` |
| `@rsbuild/core` | 见[版本矩阵](#version-matrix) |

</div>

`@rsbuild/core` 是 peer dependency 而非固定依赖,因为它间接绑定的 `@rspack/core` 必须与 `next-rspack` 引入的那一份一致。具体安装版本见下方矩阵。

## 版本矩阵 {#version-matrix}

`@rsbuild/core` 会 **exact-pin** 一个 `@rspack/core`。这个 pin 必须与 `next-rspack` 引入的 `@rspack/core` 匹配 —— 否则 Next.js 抽取出的 plugins 会绑定到与 Storybook 实际编译所用不同的 `NormalModule` 类,它们的 hooks 永远不会触发,失败会以难以理解的运行时错误浮现。为防止这一点,framework 会在启动时执行一次不变量检查,并在两份 `@rspack/core` 不一致时**抛错并附上本表的链接**。

请选择与你 `next` 版本匹配的那一行,并安装列出的 `@rsbuild/core`:

<div style={{ width: '100%', maxWidth: '700px' }}>

| `next` | `next-rspack` | `@rspack/core`(间接) | `@rsbuild/core` |
| :--- | :--- | :--- | :--- |
| `15.3.x` | 同 `next` | `1.3.4` | `1.3.6` |
| `15.4.x` | 同 `next` | `1.4.5` | `1.4.5` |
| `15.5.x` | 同 `next` | `1.4.5` | `1.4.5` |
| `16.0.x` | 同 `next` | `1.6.0` | `1.6.0` 或 `1.6.1` |
| `16.1.x` | 同 `next` | `1.6.7` | `1.6.14` |
| `16.2.x` | 同 `next` | `1.6.7` | `1.6.14` |

</div>

说明:

- `next-rspack` 必须与 `next` 安装完全相同的版本(单一发布列车,不存在跨版本兼容)。
- `@rsbuild/core` 把 `@rspack/core` pin 到一个确切版本(而非范围),所以只有上表中列出的特定 `@rsbuild/core` 版本才会解析到目标 `@rspack/core`。在同一 minor 内安装更新的 `@rsbuild/core` 通常会改变 `@rspack/core` 的 pin 并破坏不变量。
- 解析链取决于 Next 大版本:**Next 16+** 为 `next-rspack → @next/rspack-core → @rspack/core`;**Next 15** 则是 `next-rspack` 直接依赖 `@rspack/core`。
- 如果 framework 因 `@rspack/core` 不匹配而中止,请对比它打印的两个路径 —— 它们应当解析到同一个文件。受支持的修复方式是:按本表重新对齐,或用 pnpm [`overrides`](https://pnpm.io/package_json#pnpmoverrides) / yarn [`resolutions`](https://yarnpkg.com/configuration/manifest#resolutions) 把 `@rspack/core` 强制到目标版本。
- 如果两边的版本*字符串*不同但仍能兼容解析,抽取过程中可能会记录一条**非致命**的对齐 warning —— 它只是提示,不会阻断启动。

## 快速开始 {#getting-started}

### 安装 {#installation}

把 `storybook-next-rsbuild` 和 `next-rspack` 一起安装。Next.js 在构建时会内部调用 `require('next-rspack/rspack-core')`,所以 `next-rspack` 必须能从你的项目中解析到 —— 缺失这个 peer 会在 Storybook 启动时表现为一个晦涩的 module-not-found 错误。

<PackageManagerTabs command="install storybook-next-rsbuild next-rspack -D" />

让 `next-rspack` 与 `next` 保持同一版本。

如果 bridge 无法加载 Next.js 的配置(例如未安装 `next-rspack`),Storybook 仍会**仅以 React 支持启动**并记录一条安装提示 —— 但在修好 bridge 之前,CSS、字体、图片以及 navigation mocks 都不会工作。

### 配置 `.storybook/main.ts` {#configure-storybookmaints}

```ts twoslash title=".storybook/main.ts"
// @noErrors
import type { StorybookConfig } from 'storybook-next-rsbuild'

const config: StorybookConfig = {
  framework: 'storybook-next-rsbuild',
  stories: ['../src/**/*.stories.@(js|jsx|mjs|ts|tsx)'],
  addons: ['@storybook/addon-docs'],
  // Needed if stories reference assets from Next.js's `public/` dir
  // (e.g. <Image src="/vercel.svg" />).
  staticDirs: ['../public'],
}

export default config
```

这样就够了 —— framework 会自动探测项目根目录下的 `next.config.{js,ts,mjs}`。如果你的配置在别处,设置 `nextConfigPath`:

```ts twoslash title=".storybook/main.ts"
// @noErrors
import type { StorybookConfig } from 'storybook-next-rsbuild'

const config: StorybookConfig = {
  framework: {
    name: 'storybook-next-rsbuild',
    options: {
      nextConfigPath: '../next.config.ts',
    },
  },
  stories: ['../src/**/*.stories.@(js|jsx|mjs|ts|tsx)'],
}

export default config
```

相对的 `nextConfigPath` 以 Storybook 配置目录(`.storybook`)为基准解析;绝对路径则原样使用。

## 配置心智模型 {#configuration-mental-model}

`storybook-next-rsbuild` 用 Next.js 自己的 config loader 加载你的 `next.config.{js,ts}`，请 `next-rspack` 产出完整的构建配置，再把得到的 aliases、loaders、defines 和 plugins 合并进 Rsbuild。由此得出最需要内化的一点:

> **凡是 Next.js 负责编译的东西 → 写在 `next.config.ts`(Next.js 风格)。**
> **凡是属于 Storybook preview 构建本身的东西 → 写在 `.storybook/main.ts`(通过 `rsbuildFinal` 的 Rsbuild 风格,或通过 `webpackFinal` 的 webpack 风格)。**

没有第三种 framework 专属的配置面需要学习或保持同步。让 `next dev` 跑通的那套配置,已经足以让 Storybook 跑通,因为同一份 `next.config.ts` 同时驱动两者 —— 在 **dev**(`storybook dev`)和 **production**(`storybook build`)两种模式下都是如此,它们会以匹配的 mode 抽取 Next.js 的配置。

### 各自的归属 {#who-owns-what}

| 关注点 | 归属 | 配置位置 | 风格 |
| :--- | :--- | :--- | :--- |
| `'use client'` / `server-only`、JSX runtime、SWC transforms | Next.js | `next.config.ts`(自动) | Next.js |
| `transpilePackages`、`optimizePackageImports` | Next.js | `next.config.ts` | Next.js |
| 环境变量(`NEXT_PUBLIC_*`、`next.config.env`、`.env*` 文件) | Next.js | `next.config.ts` / `.env*`(自动) | Next.js |
| `compiler.*`(如 `styledComponents`、`emotion`) | Next.js | `next.config.ts` | Next.js |
| 模块 `resolve.alias` / `fallback`、自定义 loaders(如 SVGR)—— `react`* 系列 alias 除外([由 Storybook 固定](#what-gets-bridged-from-nextconfigwebpack)) | Next.js | `next.config.ts` → `webpack()` | Next.js |
| `next/font`、`next/image` | Framework(已桥接) | `next.config.ts`(自动) | Next.js |
| CSS Modules、全局 CSS、PostCSS / Tailwind | Rsbuild | 开箱即用 | — |
| Sass / Less 预处理器 | Rsbuild | `.storybook/main.ts` → `rsbuildFinal` | Rsbuild |
| 仅 Storybook preview 需要的构建微调 | Storybook builder | `.storybook/main.ts` → `rsbuildFinal` / `webpackFinal` | Rsbuild / webpack |

这种划分遵循 bridge 的设计 —— 每个工具各司其长:**Next.js 负责 JS/TS 编译与模块解析;Rsbuild 负责 CSS 管线;Storybook 负责 preview 与 React runtime。** 完整的设计原理(Next.js、Rspack、Rsbuild、Storybook 之间的「四方契约」)见 [`packages/framework-next/AGENTS.md`](https://github.com/rstackjs/storybook-rsbuild/blob/main/packages/framework-next/AGENTS.md)。

### 一项设置该写在哪里 {#deciding-where-a-setting-goes}

1. **为了让 `next dev` / `next build` 跑通,你会把它写进 `next.config.ts` 吗?** 那就留在那里 —— 它会被自动桥接。不要再复制一份到 `.storybook`。
2. **它是一个需要插件支持的 CSS 预处理器(Sass、Less、Stylus)吗?** 通过 `rsbuildFinal` 添加对应的 Rsbuild 插件。在 Storybook 中跑 CSS 管线的是 Rsbuild 而非 Next.js,所以你要按 Rsbuild 的方式来扩展。(CSS Modules、纯 CSS,以及 PostCSS/Tailwind 无需任何配置。)
3. **它是只有 Storybook preview 才需要的微调吗**(只给 stories 用的 alias、某条规则的改写)? 加 alias 或 Rsbuild 插件用 `rsbuildFinal`;只有要改写一条已存在的 rspack 规则时才用 `webpackFinal` —— 见 [`rsbuildFinal` vs `webpackFinal`](#rsbuildfinal-vs-webpackfinal)。

### `rsbuildFinal` vs `webpackFinal` {#rsbuildfinal-vs-webpackfinal}

两个 hook 都用于扩展 Storybook preview 构建,但它们**并不可互换** —— 按层级(altitude)来选:

- **`rsbuildFinal`** 是首选的高层配置面。优先用它:添加 Rsbuild 插件(`pluginSass()`)、`source.define` 条目,或只给 stories 用的 `resolve.alias`。这也是 framework 自身类型所暴露的配置面。
- **`webpackFinal`** 是底层逃生口。仅当你必须**查看或改写一条已存在的 rspack 规则**时才用它 —— 例如为 [SVGR](#advanced-custom-loaders-that-span-both-layers-eg-svgr) 把 `.svg` 从 Rsbuild 默认 asset 规则里夺走。与标准 builder 不同,这里的 `webpackFinal` 在 rspack 配置**完全组装好之后才晚一步运行**,所以你想读取的规则那时已经存在。

### 与 `@storybook/nextjs-vite` 相同 vs. 此处不同 {#same-as-storybooknextjs-vite-vs-different-here}

运行时行为 —— decorators、mocks 以及编写 story 的 API —— 都移植自 [`@storybook/nextjs-vite`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite)。因此对于下表左列的一切,**请直接 follow 官方 Storybook 文档**,本页只给出链接。右列才是本 framework 真正不同的地方,下文会详细展开。

| ✅ 相同 —— follow 官方文档(已链接) | ⚠️ 此处不同 —— 请读下文对应小节 |
| :--- | :--- |
| [`next/image`](#nextimage)、[`next/font`](#nextfont)、[`next/head`](#nexthead)、[`next/link`、`next/dynamic`](#nextlink--nextdynamic) | Rsbuild 负责 CSS 管线 → [Sass / Less 需手动开启](#sass--less) |
| story 的 routing/navigation **参数**与 [segments](#route-params--layout-segments) | [两个 router 始终都挂载](#routing--navigation) —— `appDirectory` 无效 |
| [CSS Modules、Tailwind/PostCSS、styled-jsx](#css-css-modules-postcss--tailwind-styled-jsx) | [版本矩阵与 `@rspack/core` 不变量](#version-matrix) |
| [绝对导入、aliases、subpath imports、tsconfig paths](#imports-aliases--tsconfig-paths) | [`next.config.webpack()` 的 plugins 受选项门控](#what-gets-bridged-from-nextconfigwebpack) |
| [`*.mock` subpath API](#mocking-nextjs-apis-in-stories)(`cache`/`headers`/`navigation`/`router`) | [`node:` 协议与 Node 内置模块](#node-protocol--node-builtins)的处理 |
| [SWC transforms、`transpilePackages`、`optimizePackageImports`](#swc-transforms-transpilepackages-optimizepackageimports) | [Next 16+ 不再支持 runtime config](#runtime-config) |

> **经验法则:** 如果某个 Next.js 或 Storybook 特性在本页完全没有提及,默认按官方 [`@storybook/nextjs-vite`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite) 文档来 —— runtime 正是移植自它。唯一不能照搬的是**构建/打包配置**:请忽略上游的 `viteFinal` 指引,改为通过 `next.config.ts`(桥接)加 `rsbuildFinal` / `webpackFinal` 来驱动构建,如上所述。

## Framework 选项 {#framework-options}

<div style={{ width: '100%', maxWidth: '760px' }}>

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `nextConfigPath` | `string` | 自动探测 | `next.config.{js,ts,mjs}` 文件的路径。可为绝对路径,或相对于 `.storybook` 的相对路径。 |
| `forwardNextConfigPlugins` | `boolean` | `false` | 把 `next.config.webpack()` 添加的 **plugins** 转发进 Storybook 的 bundler。rules、aliases、fallbacks 和 externals *始终*会被转发 —— 只有 plugins 受门控。见[下文](#what-gets-bridged-from-nextconfigwebpack)。 |
| `image` | `object` | `{}` | 为与 `@storybook/nextjs` 兼容而接受;当前运行时的 `next/image` mock 会回退到上游默认值(未接线)。单条 story 的图片配置通过 `parameters.nextjs.image` 设置,而非此选项。 |
| `builder` | `BuilderOptions` | `{}` | 转发给 `storybook-builder-rsbuild` 的选项。见[配置指南](/guide/configuration#builder-options)。 |

</div>

TypeScript 相关设置(包括 `typescript.reactDocgen`)会在 `main.ts` 中通过类型检查,行为与[配置指南](/guide/configuration)中所述一致。

## 从 `next.config.webpack()` 桥接了什么 {#what-gets-bridged-from-nextconfigwebpack}

如果你的项目在 `next.config.ts` 中自定义了 webpack,bridge 会自动转发你的 **rules、aliases、fallbacks、externals 和 experiments** —— 它们被视为 Next.js 拥有的配置并原样复用。

**plugins 是例外。** 它们受 `forwardNextConfigPlugins` 选项门控(默认 `false`),因为人们添加的大多数 plugins 都针对 Next.js 的生产管线(build-manifest 写入、source-map 上传、stats 输出),在 Storybook 中要么无作用,要么会在 `processAssets` 阶段把 rspack 的 IPC channel 搞崩(如 `copy-webpack-plugin` 的 `processAssets` hook)。门控关闭时,被丢弃的 plugins 会按名称记录。只有当某个 client-side plugin 经你针对 `@rspack/core` 验证可用时,才开启它:

```ts twoslash title=".storybook/main.ts"
// @noErrors
import type { StorybookConfig } from 'storybook-next-rsbuild'

const config: StorybookConfig = {
  framework: {
    name: 'storybook-next-rsbuild',
    options: {
      // Forward DefinePlugin / client-side plugins from next.config.webpack().
      forwardNextConfigPlugins: true,
    },
  },
  stories: ['../src/**/*.stories.@(js|jsx|mjs|ts|tsx)'],
}

export default config
```

**除 plugins 外还有两处刻意过滤的例外:**

- **`react` / `react-dom` / `react-server-dom-webpack` 的 alias**:在 `next.config.webpack()` 里设置的这几个 alias 会被丢弃(并记录一条 warning)。Storybook 拥有 React singleton —— 出现第二份 React 会破坏 hooks 和 context —— 因此刻意不提供重新指向 React 的逃生口。
- **`.mdx` 规则**(如来自 `@next/mdx`):会被丢弃(并记录一条 info 日志)。在 Storybook 中,`.mdx` 由 `@storybook/addon-docs` 拥有。你的 page-MDX loader 仍然作用于真实的 Next.js 页面,只是不作用于 Storybook docs。

## 支持的 Next.js 特性 {#supported-nextjs-features}

### `next/image` {#nextimage}

`next/image` 可以在 stories 中渲染。framework 把 Next.js 默认的图片 loader 换成了一个 client-side mock,因此 stories 不依赖 `/_next/image` 优化端点 —— 本地与远程的 `src` 都能直接渲染,也无需安装 `sharp`。

用法、本地/远程的行为差异,以及「图片导入返回的是一个对象」这一规则,都与上游完全一致 —— **见 [Next.js's Image component](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjss-image-component)**。本 framework 特有的两点:

- 像 `/vercel.svg` 这样的本地 `src` 只有在你把 Next.js 的 `public/` 目录加入 `staticDirs` 时才能解析(见 [main.ts 示例](#configure-storybookmaints))。
- 单条 story 的 `next/image` 配置通过 `parameters.nextjs.image`(一个 `ImageConfigContext` 值)应用,且仅在设置时生效。

```tsx twoslash title="Image.stories.tsx"
// @noErrors
import Image from 'next/image'
import type { Meta, StoryObj } from 'storybook-next-rsbuild'

const meta = {
  component: Image,
  args: { src: '/vercel.svg', alt: 'Vercel', width: 200, height: 48 },
} satisfies Meta<typeof Image>

export default meta

export const Default: StoryObj<typeof meta> = {}
```

### `next/font` {#nextfont}

`next/font/google` 和 `next/font/local` 都开箱即用,且无需 `staticDirs` 映射。这是 **framework 唯一自己接管的 CSS 关注点**:`next-swc` 为每个 `next/font` 调用产出的合成 `target.css`,会被路由到一个专用的 font loader,在运行时注入 `@font-face`/class CSS;其余一切仍走 Rsbuild 的 CSS 管线。

所支持的范围 —— 包括*不支持*的选项(`fallback`、`adjustFontFallback`、`preload`/`display` 被忽略)以及 `NEXT_FONT_GOOGLE_MOCKED_RESPONSES` 的 CI mocking 建议 —— 都与上游一致。**见 [Next.js font optimization](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjs-font-optimization)。**

```tsx twoslash title="Font.stories.tsx"
// @noErrors
import { Inter } from 'next/font/google'
import type { Meta, StoryObj } from 'storybook-next-rsbuild'

const inter = Inter({ subsets: ['latin'] })

function FontDemo({ className }: { className: string }) {
  return <p className={className}>The quick brown fox jumps over the lazy dog.</p>
}

const meta = { component: FontDemo } satisfies Meta<typeof FontDemo>
export default meta

export const Default: StoryObj<typeof meta> = {
  args: { className: inter.className },
}
```

### `next/head` {#nexthead}

通过一个内置 decorator 更新 `document.head`,开箱即用。children 会落到 preview iframe 的 `<head>` 中,与上游所述一致 —— **见 [Next.js Head](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjs-head)**。

### `next/link` 与 `next/dynamic` {#nextlink--nextdynamic}

两者都原样工作。`next/link` 通过 mock 的 router 进行导航(见 [Routing](#routing--navigation));`next/dynamic` 的 lazy chunks 无需特殊接线即可解析。Routing 行为见上游文档 —— **见 [Next.js routing](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjs-routing)**。

## 路由与导航 {#routing--navigation}

> **两个 router 始终都处于激活状态。** 与 `@storybook/nextjs-vite` 不同,本 framework 在**每一条** story 上都挂载 App Router(`next/navigation`)和 Pages Router(`next/router`)的 context —— 没有 router 选择器。这里的 `parameters.nextjs.appDirectory` 标志**没有任何作用**(也不在导出的类型里),因此一条 story 可以独立地读取 `next/navigation` 和 `next/router`。这是刻意为之:混合路由(Next.js 13+)的项目正需要如此。

如果你正从 `@storybook/nextjs-vite` 迁移,**请从参数中移除 `appDirectory`** —— 它会被接受但忽略,App/Pages 的 hooks 无论如何都能工作。

### App Router —— `next/navigation` {#app-router--nextnavigation}

通过 `parameters.nextjs.navigation`(`pathname`、`query` 和 `segments`)来给 `usePathname`、`useSearchParams`、`useParams` 以及 layout-segment 系列 hooks 读取的 navigation context 注入初始值。hook 行为与默认 context(`{ pathname: '/', query: {} }`)都继承自上游 —— **见 [Next.js navigation](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjs-navigation)**。

```tsx twoslash title="Navigation.stories.tsx"
// @noErrors
import { useRouter, usePathname, useSearchParams } from 'next/navigation'
import type { Meta, StoryObj } from 'storybook-next-rsbuild'

function Component() {
  const router = useRouter()
  const pathname = usePathname()
  const searchParams = useSearchParams()

  return (
    <button type="button" onClick={() => router.push('/next')}>
      Navigate from {pathname}?{searchParams.toString()}
    </button>
  )
}

export default {
  component: Component,
  parameters: {
    nextjs: {
      // No `appDirectory` needed — both routers are always mounted.
      navigation: {
        pathname: '/hello',
        query: { foo: 'bar' },
      },
    },
  },
} satisfies Meta<typeof Component>

export const Default: StoryObj<typeof Component> = {}
```

### 路由参数与 layout segments {#route-params--layout-segments}

`useSelectedLayoutSegment`、`useSelectedLayoutSegments` 和 `useParams` 由 `parameters.nextjs.navigation.segments` 驱动,它接受两种形式:

- **`string[]`** —— 一条用于构建 layout-segment 树的 parallel-route 路径,例如 `segments: ['dashboard', 'analytics']`。
- **`[key, value][]` 元组**(或一个普通对象)—— `useParams()` 返回的显式路由参数,例如 `segments: [['address', '0xdeadbeef']]` → `useParams()` 返回 `{ address: '0xdeadbeef' }`。

hook 的返回语义与上游一致 —— **见 [`useSelectedLayoutSegment(s)` / `useParams` hooks](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#useselectedlayoutsegment-useselectedlayoutsegments-and-useparams-hooks)**。

```tsx twoslash title="RouteParams.stories.tsx"
// @noErrors
import { useParams } from 'next/navigation'
import type { Meta, StoryObj } from 'storybook-next-rsbuild'

function Profile() {
  const { address } = useParams()
  return <span>Address: {address}</span>
}

export default {
  component: Profile,
  parameters: {
    nextjs: {
      navigation: { segments: [['address', '0xdeadbeef']] },
    },
  },
} satisfies Meta<typeof Profile>

export const Default: StoryObj<typeof Profile> = {}
```

### Pages Router —— `next/router` {#pages-router--nextrouter}

Pages Router 的 stories 通过 `parameters.nextjs.router` 注入初始值。接受的形状与默认 router 状态(`pathname: '/'`、`isReady: true` 等)都继承自上游 —— **见 [Next.js routing](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#nextjs-routing) 与 [default router](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#default-router)**。

```tsx twoslash title="PagesRouter.stories.tsx"
// @noErrors
import { useRouter } from 'next/router'
import type { Meta, StoryObj } from 'storybook-next-rsbuild'

function Component() {
  const router = useRouter()
  return <span>Path: {router.pathname}</span>
}

export default {
  component: Component,
  parameters: {
    nextjs: {
      router: { pathname: '/pages-route', query: { id: '42' } },
    },
  },
} satisfies Meta<typeof Component>

export const Default: StoryObj<typeof Component> = {}
```

### 参数参考 {#parameters-reference}

<div style={{ width: '100%', maxWidth: '760px' }}>

| Parameter | 适用于 | 形状 | 说明 |
| :--- | :--- | :--- | :--- |
| `nextjs.navigation` | App Router(`next/navigation`) | `{ pathname?, query?, segments? }` | 导出的类型是 `Partial<NextRouter>`,但运行时该对象同样接受 `segments`(路由参数/segment 的驱动)。 |
| `nextjs.router` | Pages Router(`next/router`) | `Partial<NextRouter>` | 给 stub 的 Pages Router 注入初始值。 |
| `nextjs.appDirectory` | — | — | **接受但忽略。** 两个 router 始终都挂载;保留它只是为了与 `nextjs-vite` 的源码兼容。 |

</div>

## 样式 {#styling}

### CSS、CSS Modules、PostCSS / Tailwind、styled-jsx {#css-css-modules-postcss--tailwind-styled-jsx}

**Rsbuild 负责 CSS 管线**(这是[心智模型](#configuration-mental-model)里刻意的分工)。CSS Modules、全局 CSS 导入、PostCSS 和 Tailwind 无需额外接线即开箱即用,行为与你的 Next.js 应用一致 —— 处理它们的是 Rsbuild,而非一条桥接来的 Next.js CSS 链。`styled-jsx` 也能工作;它是一个走 Next.js loader 链的 SWC transform。

用法与上游一致 —— **见 [CSS Modules](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#csssassscss-modules)、[Tailwind](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#tailwind) / [PostCSS](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#postcss),以及 [Styled JSX](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#styled-jsx)**。唯一要知道的是*归属*:因为跑管线的是 Rsbuild(而非 Next.js),自定义的 PostCSS/Tailwind 配置会被 Rsbuild 的自动探测拾取,而预处理器则需手动开启 —— 见下文。

### Sass / Less {#sass--less}

这是**与官方 Next.js Storybook 行为不同**的唯一一项样式特性。上游继承了 Next.js 内置的 Sass 支持,零配置即可;而这里因为 Rsbuild 负责 CSS 管线,Sass 和 Less 需**通过一个 Rsbuild 插件手动开启**,且 `next.config` 的 Sass 选项**不会**流经。如果你在没有配置 Sass loader 的情况下导入 `.scss`/`.sass` 文件,framework 会发出一条一次性 warning 指回这里。

安装插件并通过 `rsbuildFinal` 合并:

<PackageManagerTabs command="install @rsbuild/plugin-sass -D" />

```ts twoslash title=".storybook/main.ts"
// @noErrors
import { mergeRsbuildConfig } from '@rsbuild/core'
import { pluginSass } from '@rsbuild/plugin-sass'
import type { StorybookConfig } from 'storybook-next-rsbuild'

const config: StorybookConfig = {
  framework: 'storybook-next-rsbuild',
  stories: ['../src/**/*.stories.@(js|jsx|mjs|ts|tsx)'],
  // Rsbuild owns the CSS pipeline — extend it the Rsbuild way. The plugin
  // merges into the config the framework's bridge produces.
  rsbuildFinal: (config) =>
    mergeRsbuildConfig(config, { plugins: [pluginSass()] }),
}

export default config
```

选择与你[版本矩阵](#version-matrix)那一行所 pin 的 `@rsbuild/core` 兼容的 `@rsbuild/plugin-sass` 版本。Less 的方式相同,使用 `@rsbuild/plugin-less`。

### CSS-in-JS(styled-components 走 SWC,Emotion 运行时) {#css-in-js-styled-components-via-swc-emotion-at-runtime}

它们是 Next.js 的 compiler transforms,所以你按 Next.js 的方式 —— 在 `next.config.ts` 里 —— 启用它们,它们会流经桥接来的 `next-swc` 链。无需 Storybook 侧接线:

```ts twoslash title="next.config.ts"
// @noErrors
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  compiler: {
    styledComponents: true,
  },
}

export default nextConfig
```

Emotion 不需要任何特殊 transform —— 它是运行时 CSS-in-JS,原样工作。(Next.js 的 `compiler.emotion` SWC transform 是可选的,启用后同样会被桥接。)

## 编译与模块解析 {#compilation--module-resolution}

### SWC transforms、`transpilePackages`、`optimizePackageImports` {#swc-transforms-transpilepackages-optimizepackageimports}

stories 用 Next.js 自己的 `next-swc-loader` 编译,所以构建行为与你的应用一致:

- `'use client'` 指令的行为与在 Next.js 中一致,`server-only` 模块也像真实构建一样解析。
- `next.config.ts` 中的 `transpilePackages` 条目会自动应用到 stories。
- `optimizePackageImports`(Next 15+ 默认开启)被支持 —— `next-swc` 发出的 `__barrel_optimize__` 请求会被路由进 SWC 链,因此即便是 TypeScript 源码的 barrel 也能正确编译。
- JSX runtime 的选择跟随你的 `next.config.ts`。

这些都是 Next.js 拥有的关注点:在 `next.config.ts` 里配置,即被桥接。(TypeScript 行为与上游一致 —— **见 [Typescript](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#typescript)**。)

### 导入、aliases 与 tsconfig paths {#imports-aliases--tsconfig-paths}

根相对的绝对导入、模块 aliases(`@/...`)、Node 标准的 subpath imports(来自 `package.json#imports` 的 `#...`),以及 `tsconfig.json` 的 `baseUrl`/`paths` 都能解析,因为 bridge 转发了 Next.js 已解析的 aliases。其行为 —— 以及「绝对导入无法被 mock」这一注意点 —— 都与上游一致。**见 [Imports](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#imports)。**

### 环境变量 {#environment-variables}

`NEXT_PUBLIC_*` 变量与 `next.config.ts` 的 `env` 键会自动抵达你的 stories:Next.js 通过它的 `DefinePlugin` 把它们编译进 `process.env.*`,bridge 再把这些 defines 原样转发给 Rsbuild。`.env`、`.env.local`、`.env.development` 里的值也会被读取,因为 framework 是通过 Next.js 自己的 config loader(它会运行 `@next/env`)来加载配置的。所以无需再用 `rsbuildFinal` 的 `source.define` 重新定义。

有一处限制与真实构建一致:**server-only** 环境变量 —— 即*不带* `NEXT_PUBLIC_` 前缀的那些 —— 不会被 inline 进 client bundle,因此在 stories 中读到的是 `undefined`,这与 `next build` 下 client 组件的行为完全相同。

### `node:` 协议与 Node 内置模块 {#node-protocol--node-builtins}

在面向浏览器的代码中导入 Node 内置模块不会让 Storybook 构建崩溃。裸内置模块(`fs`、`path`、`querystring` 等)会解析到一个空 fallback,带 `node:` 前缀的 specifier 会在解析前被规范化:像 `node:path` 这样的内置模块会被剥去前缀变为裸名;而没有裸名对应物的 `node:`-only specifier(`node:sqlite`、`node:test`)则解析为一个空模块。Next.js 的 `Buffer`/`process` provides 也被保留,因此 `next-auth` / `openid-client` 之类的库不会在渲染时抛出 `Buffer is not defined`。这是 framework 特有、上游没有对应物的行为 —— 无需任何配置。

### 进阶:横跨两层的自定义 loaders(如 SVGR) {#advanced-custom-loaders-that-span-both-layers-eg-svgr}

少数场景需要同时改动**两层**配置,因为 Next.js 的 webpack 配置与 Storybook 的 preview 配置彼此看不见。SVGR 是典型例子:你按 Next.js 的方式添加 loader 规则(于是它被桥接),但你还得通过 `webpackFinal` 把 `.svg` 从 Rsbuild 默认的 asset 规则里夺走 —— 这是 preview 侧的事。framework 会在 rspack 配置**完全组装好之后,才**晚一步运行你的 `webpackFinal`,所以对已有规则的查看与改写都能生效。(如果你的 `webpackFinal` 添加的某条规则,其 `test` 与一条 `next.config` 添加的规则匹配,framework 会丢弃重复的那条,以避免处理两遍。)

```ts twoslash title="next.config.ts"
// @noErrors
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  webpack: (config) => {
    config.module?.rules?.push({ test: /\.svg$/, use: ['@svgr/webpack'] })
    return config
  },
}

export default nextConfig
```

```ts twoslash title=".storybook/main.ts"
// @noErrors
import type { StorybookConfig } from 'storybook-next-rsbuild'

const config: StorybookConfig = {
  framework: 'storybook-next-rsbuild',
  stories: ['../src/**/*.stories.@(js|jsx|mjs|ts|tsx)'],
  // Exclude .svg from Rsbuild's default asset rule so the SVGR rule added in
  // next.config.webpack() is the one that processes them.
  webpackFinal: async (config) => {
    for (const rule of config.module?.rules ?? []) {
      if (
        rule &&
        typeof rule === 'object' &&
        rule.test instanceof RegExp &&
        rule.test.test('probe.svg')
      ) {
        ;(rule as any).exclude = /\.svg$/
      }
    }
    return config
  },
}

export default config
```

## 在 stories 中 mock Next.js 的 API {#mocking-nextjs-apis-in-stories}

为了交互测试和手动覆盖,framework 提供了一组与 `@storybook/nextjs-vite` 对应的 subpath 导出。每个入口都 re-export 真实的 Next.js 模块,并用来自 `storybook/test` 的可 spy 的 [`fn()`](https://storybook.js.org/docs/writing-tests/interaction-testing) mock 包裹部分 API。**只有 package 名不同** —— API 表面与行为都是原样移植的,所以上游参考同样适用(下表每行已给出链接)。

<div style={{ width: '100%', maxWidth: '820px' }}>

| Import | 用于 | 上游参考 |
| :--- | :--- | :--- |
| `storybook-next-rsbuild/navigation.mock` | App Router —— `useRouter`、`usePathname`、`useSearchParams`、`redirect`、`notFound`、`useParams` 等 + `getRouter()` | [`navigation.mock`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#storybooknextjs-vitenavigationmock) |
| `storybook-next-rsbuild/router.mock` | Pages Router —— `useRouter`、`withRouter`、单例 router + `getRouter()` | [`router.mock`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#storybooknextjs-viteroutermock) |
| `storybook-next-rsbuild/cache.mock` | `revalidatePath`、`revalidateTag`、`unstable_cache`、`unstable_noStore` | [`cache.mock`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#storybooknextjs-vitecachemock) |
| `storybook-next-rsbuild/headers.mock` | `headers`、`cookies`、`draftMode`(可写:`headers().set(...)`、`cookies().set(...)`) | [`headers.mock`](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#storybooknextjs-viteheadersmock) |

</div>

在 `play` 函数里调用 `getRouter()` 来断言 router 的交互。App Router 的 stories 用 `navigation.mock`,Pages Router 的 stories 用 `router.mock`:

```tsx twoslash title="Navigation.stories.tsx"
// @noErrors
import { expect, userEvent, within } from 'storybook/test'
import { getRouter } from 'storybook-next-rsbuild/navigation.mock'
import type { StoryObj } from 'storybook-next-rsbuild'

export const Default: StoryObj = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement)
    const router = getRouter()

    await userEvent.click(canvas.getByRole('button'))
    await expect(router.push).toHaveBeenCalledWith('/next')
  },
}
```

要 mock 你**自己的**(非 Next.js)模块,使用 Storybook 的 [module mocking](https://storybook.js.org/docs/writing-stories/mocking-data-and-modules/mocking-modules) 指南。

:::warning 注意事项
- **单例状态。** `getRouter()` 返回的是最近一次 story 渲染所注入的实例。不要跨 story 持有这个引用 —— 在每个 `play` 里都重新读取。
- **仅限 client-side。** Storybook 不运行 Next.js 服务器,所以当 client 组件在 stories 中导入 server-only 的 API 时,`cache.mock` 和 `headers.mock` 是它们唯一能解析的途径。
- **耦合 Next.js 内部实现。** 这些 subpath 通过 `next/dist/*` 路径 re-export。未来某个 `next` 版本可能重命名它们,通常需要升级 `storybook-next-rsbuild`。
:::

## Runtime config {#runtime-config}

`getConfig()` 和 `publicRuntimeConfig` 原则上可用 —— 因为 Storybook 不做服务端渲染,组件看到的是 `publicRuntimeConfig`(而非 `serverRuntimeConfig`),与上游一致(**见 [Runtime config](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#runtime-config)**)。

有一处差异:对 `next/config` 的旧式支持是**按版本门控**的。Next.js 16 把 `next/config` 从其 package exports 中移除了,因此在 **Next 16+ 上实际上不再支持 runtime config**;它只在 Next 15 上接线。

## 已知限制 {#known-limitations}

- **无 Server Components 运行时。** 标记了 `'use client'` 的组件会渲染;纯 Server Components 不会执行。注意本 framework **不**提供 [`@storybook/nextjs-vite` 所记录](https://storybook.js.org/docs/get-started/frameworks/nextjs-vite#react-server-components-rsc)的 `experimentalRSC` Suspense 包裹路径 —— 只有 client 组件会渲染。
- **无 API routes、middleware 或 server actions。** Storybook 不运行 Next.js 服务器 —— `route.ts`、`middleware.ts` 和 `'use server'` 入口都不会执行。(与 `@storybook/nextjs-vite` 相同。)
- **无 `/_next/image` 优化。** `next/image` 使用 client-side 的 mock loader;运行时行为与生产环境(图片即时优化)不同。
- **Sass/Less 需要一个 Rsbuild 插件。** 预处理器支持需通过 `rsbuildFinal` 手动开启,而非从 Next.js 继承(见 [Sass / Less](#sass--less))。
- **Next 16+ 不再支持 runtime config**(见 [Runtime config](#runtime-config))。
- **部署/输出类配置不生效。** `output`(`export`/`standalone`)、`basePath`、`assetPrefix`、`trailingSlash` 以及 `rewrites`/`redirects`/`headers` 在 Storybook 中无任何作用 —— preview 在根路径下提供服务,所以 story 的资源路径(`staticDirs`、`next/image` 的 `src`)**无需**加 `basePath` 前缀。
- **版本耦合。** bridge 依赖 Next.js 内部模块。任何 `next` 的 patch 或 minor 发布都可能破坏兼容性 —— 请让 `storybook-next-rsbuild` 与 `next` 一起升级。

## 后续步骤 {#next-steps}

- **[编写 Stories](https://storybook.js.org/docs/writing-stories)**:了解 Component Story Format(CSF)的基础知识。
- **[交互测试](https://storybook.js.org/docs/writing-tests/interaction-testing)**:断言上文的 router mocks。
- **[配置指南](/guide/configuration)**:`rsbuildFinal`、builder 选项以及 TypeScript 设置。

仓库中 [`sandboxes/nextjs`](https://github.com/rstackjs/storybook-rsbuild/tree/main/sandboxes/nextjs) 提供了一份完整、可运行的参考,覆盖 App Router、Pages Router、`next/font`、`next/image`、CSS Modules、Tailwind、Sass、styled-components、Emotion、`optimizePackageImports`、`transpilePackages`、SVGR,以及用户的 `next.config.webpack()` 增量。
